import java.io.*;
import java.util.*;

public class Solution {

    public static void main(String[] args) {
        /* Enter your code here. Read input from STDIN. Print output to STDOUT. Your class should be named Solution. */
        Scanner input = new Scanner(System.in);
        int pilihan = input.nextInt();
        if (pilihan == ){
            int s = input.nextInt();
            System.out.println(persegi(s));
        }
        else if (pilihan == 2){
            doubel alas = input.nextDouble();
            doubel tinggi = input.nextDouble();
            System.out.println(segitiga(alas, tinggi));
        }
        else if (pilihan == 3){
            double jari = input.nextDouble();
            if (jari%7 == 0){
                System.out.println(lingkaran(jari));
            }
            else 
              System.out.println(lingkaran2(jari));
        }
        else System.out.println("Input yang anada masukan tidak valid");
    }
    public static int persegi(int sisi){
        int hasil = sisi*sisi;
        return hasil;
    }
    public static int segitiga (Double a, Double t){
        int hasil = (int) (a*t/2);
        return hasil;
    }
    public static double lingkaran(double a, Double r){
        double hasil =22 / 7 
    }
}